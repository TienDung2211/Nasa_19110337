{
  "name": "nasa_19110337",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "install-server": "npm install server --prefix",
    "install-client": "npm install client --prefix",
    "install": "npm run install-server && npm run install-client",
    "server": "npm run watch --prefix server",
    "client": "npm start --prefix client",
    "watch": "npm run server && npm run client",
    "deploy": "npm run build --prefix client && npm start --prefix server",
    "test": "npm run test --prefix server && npm run test --prefix client"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.2.0",
    "client": "^0.0.1",
    "dotenv": "^16.0.3",
    "mongose": "^0.0.2-security",
    "morgan": "^1.10.0",
    "pm2": "^5.2.2",
    "server": "^1.0.37"
  },
  "description": "",
  "devDependencies": {
    "jest": "^29.3.0",
    "supertest": "^6.3.1"
  }
}
